import { createTheme } from "@mui/material";

export const customTheme = createTheme({
  palette: {
    primary: {
      main: "#e5e7eb",
      dark: "#d1d5db",
      light: "#e5e7eb",
    },
    secondary: {
      main: "#16a34a",
      yellow: "#f59e0b",
      blue: "#60a5fa",
    },
  },
});
